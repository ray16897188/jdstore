<div class="container">

	<% @orders.each do |order| %>

	<% delivery_address = DeliveryAddress.find(order.delivery_address_id ) %>

	<div class="row well"  style="background:url(https://ooo.0o0.ooo/2016/11/05/581dee45a871c.jpg); background-size: 100% 100%;">

		<%= link_to "查看详情", order_path(order.token), method: :get, class: "btn btn-default pull-right" %>
		
		<%= render partial: "common/order_table", locals: {delivery_address: delivery_address, order: order} %>

		<%= render partial: "customer_order_action", locals: {order: order} %>
	
	</div>

	<% end %>
	
</div>